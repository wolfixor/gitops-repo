apiVersion: v1
kind: Service
metadata:
  name: dashboard-app
  labels:
    app: dashboard-app
spec:
  type: ClusterIP
  selector:
    app: dashboard-app
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP